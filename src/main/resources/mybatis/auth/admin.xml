<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.example.demo.auth.dao.AdminDao">
  
  <select id="selectAdminPassword" parameterType="String">
    select * from admin where a????????????????어케해
  </select>

</mapper>