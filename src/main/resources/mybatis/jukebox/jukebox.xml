<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.jukebox.dao.JukeboxDao">
  
  <insert id="insert" parameterType="Jukebox" useGeneratedKeys="true" keyProperty="jid" keyColumn="jid">
    insert into jukeboxes (mid, title, content)
    values (#{mid}, #{title}, #{content})
  </insert>

  <select id="selectByJid" parameterType="Long" resultType="Jukebox">
    select * from jukeboxes where jid = #{jid}
  </select>

  <select id="selectListByMid" resultType="Jukebox">
    select * from jukeboxes
    where mid = #{mid}
    order by updated_at desc
  </select>

  <select id="countByMid" resultType="Long">
    select count(*) from jukeboxes where mid = #{mid}
  </select>

  <update id="update" parameterType="Jukebox">
    update jukeboxes
    set title = #{title},
        content = #{content},
        updated_at = sysdate
    where jid = #{jid}
  </update>

  <delete id="delete" parameterType="Long">
    delete from jukeboxes where jid = #{jid}
  </delete>


</mapper>